using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArrowTail : MonoBehaviour
{
	/*private void Update()
	{
		BuildTail();
	}
	void BuildTail()
	{
		for (int i = 0; i < transform.childCount; i++)
		{
			if (Player.weaponTail.transform.position.y > transform.GetChild(i).position.x)
			{
				transform.GetChild(i).gameObject.SetActive(true);
			}
			else if(Player.weaponTail==null)
			{
				transform.GetChild(i).gameObject.SetActive(false);
				transform.GetChild(i).position = new Vector2(transform.GetChild(i).position.x, Player.instance.transform.position.x);
			}

		} 
	}*/
}
